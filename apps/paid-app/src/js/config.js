// App configuration - identity and flags.
// Import: import { config } from './config.js';

export const config = {
  title: '{{JS_TITLE}}',
  version: 1,

  // Prices shown on the page are defined inline in index.html via the
  // <stripe-buy-button> elements (amounts are in cents). Change them there, or
  // drive them from here if you prefer a single source of truth.
  features: {},
};
