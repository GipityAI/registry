# notify-demo

Starter app for **Gipity Notify** (web push). See the top-level `README.md` for usage. The push primitives are the `notify` kit (`src/packages/notify/`) plus the platform's Gipity Notify service; this app just wires them to two buttons.
